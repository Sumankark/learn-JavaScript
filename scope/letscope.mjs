let name = "Suman"
{
  name = "karki"
  
  console.log(name)
}
console.log(name)
/* 
  Hoisting :
    before execution of code first memory s allocation for the variable ths s called hoisting.

  We cannot declare same variable twice in the same block
  
  Scope of variable.
    A variable inside its block only.
    Inside block it is known from where it is define.

*/