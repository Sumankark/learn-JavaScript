let info = {
    // key   value  = property
    name: "suman",
    age: 23,
    name: "ram",

};

console.log(info);

// in object duplicate key does not exist
// if duplicate key exist latest ke yoverwrite the old one.