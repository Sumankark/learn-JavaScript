let ar1 = [1, 2, 3];
console.log(ar1[0]);

// in array order of element is matter.
// but in object order of element does not matter

let info = {
    name: "suman",
    address: "lahan",
    isMarred: false,
    age: 23, 
    name: "ram"
}

console.log(info.name);